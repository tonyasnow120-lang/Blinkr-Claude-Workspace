/**
 * Firebase configuration — to be filled in by collaborator.
 *
 * Steps:
 *   1. Go to Firebase Console → Project Settings → Your apps
 *   2. Add an iOS app (bundle ID: com.blinkr) and an Android app (com.blinkr)
 *   3. Download GoogleService-Info.plist → drop into ios/Blinkr/
 *   4. Download google-services.json   → drop into android/app/
 *   5. Fill in the firebaseConfig object below
 *   6. Call initializeApp() at app startup (e.g. in index.js)
 *
 * Both GoogleService-Info.plist and google-services.json are in .gitignore
 * to keep API keys out of the repo.
 */

// import { initializeApp } from '@react-native-firebase/app';

const firebaseConfig = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: '',
};

// Uncomment once collaborator fills in the config above:
// initializeApp(firebaseConfig);

export default firebaseConfig;
